<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- क्लाइंट ऐप के लिए ज़रूरी परमिशंस -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- बैकग्राउंड में चलने और फोन लॉक करने के लिए -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" /> <!-- स्क्रीन के ऊपर लॉक दिखाने के लिए -->

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="Smart Client App"
        android:roundIcon="@android:drawable/sym_def_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
                <!-- बैकग्राउंड सर्विस -->
        <service 
            android:name=".LockService" 
            android:exported="false" />

        <!-- फोन रीस्टार्ट होने पर काम करने वाला रिसीवर -->
        <receiver 
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        
        <receiver 
            android:name=".MyAdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data 
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_receiver" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>
        
        <!-- यह मेन एक्टिविटी है जो ऐप खुलने पर दिखेगी -->
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity 
            android:name=".LockScreenActivity"
            android:launchMode="singleTask"
            android:excludeFromRecents="true"
            android:exported="false" />
        
        <!-- (Device Admin और Background Service हम अगले स्टेप में यहाँ जोड़ेंगे) -->

    </application>

</manifest>

